# Agents

An **agent** wraps an LLM in a loop so it can **act** — call tools, read files, search — not just chat.

The **harness** (your Python) decides what tools exist, when they run, and what the model sees back.

Fake tools in early lessons become real file tools here. Same loop, real data.
